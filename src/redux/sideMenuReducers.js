

let initialState = {
    listsItem: [
        { id: 1, nameList: 'List 1' },
        { id: 2, nameList: 'List 2' },
        { id: 3, nameList: 'List 3' }
    ]
}

const sideMenuReducers = (state = initialState, action) => {
    return state;
}

export default sideMenuReducers;